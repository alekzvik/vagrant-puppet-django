[uwsgi]
# Define variables
project = %n
base_dir = /home/vagrant/www/<%= @project %>
virtualenv = /home/vagrant/virtualenvs/<%= @project %>/

# Config
uid = <%= @uwsgi_user %>
gid = <%= @uwsgi_group %>


socket = <%= @sock_dir %>/%(project).sock
master = true
no-orphans = true

chdir = %(base_dir)/%(project)
module = %(project).wsgi:application

logto = /var/log/uwsgi/%(project).log
